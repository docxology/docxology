# AGENTS.md — `docxology/reports/visual-qa/`

Added by the 2026-08-29 ongoing-docs fleet pass.

## What this is

Per-run visual QA outputs (currently `2026-08-26/`). Generated artifacts under `docxology/reports/`; local-only, never commit.

## Verify

- `ls docxology/reports/visual-qa` — dated run folders.
