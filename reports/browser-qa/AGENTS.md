# AGENTS.md — `docxology/reports/browser-qa/`

Added by the 2026-08-29 ongoing-docs fleet pass.

## What this is

Per-run browser QA outputs, one `YYYY-MM-DD/` folder per run (each with manifest.json). Generated artifacts under `docxology/reports/`; local-only, never commit.

## Verify

- `ls docxology/reports/browser-qa` — dated run folders.
