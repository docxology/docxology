# AGENTS.md — `docxology/reports/browser-smoke/`

Added by the 2026-08-29 ongoing-docs fleet pass.

## What this is

Per-run browser smoke-test outputs (currently `2026-08-26/`). Generated artifacts under `docxology/reports/`; local-only, never commit.

## Verify

- `ls docxology/reports/browser-smoke` — dated run folders.
